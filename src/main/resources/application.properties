quarkus.http.port=8080
quarkus.log.level=INFO
quarkus.log.category."com.mylake".level=DEBUG

# Comma-separated list of allowed filesystem path prefixes.
# When non-empty, any path parameter not starting with one of these prefixes
# is rejected with HTTP 403. Leave empty (default) to allow all paths.
mylake.allowed-paths=
